apiVersion: v1
# https://kubernetes.io/docs/tasks/configure-pod-container/configure-service-account
kind: ServiceAccount
metadata:
  name: {{ include "buch.fullname" . }}
  labels:
    account: {{ include "buch.fullname" . }}
automountServiceAccountToken: false
